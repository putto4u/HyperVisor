# VirtualBox 네트워크 모드 심층 분석: 호스트 전용 vs NAT 네트워크

VirtualBox에서 가상 머신(VM) 환경을 구축할 때 가장 중요한 것은 '네트워크 설계'입니다. 특히 **호스트 전용(Host-Only)**과 **NAT 네트워크(NAT Network)**는 실습 환경 구축 시 가장 빈번하게 사용되는 두 가지 모드이며, 그 동작 방식과 용도가 명확히 다릅니다.

---

### 1. 핵심 비교 요약표

가장 큰 차이점은 **'인터넷 연결 유무'**와 **'호스트(내 PC)와의 직접 통신 여부'**입니다.

| 비교 항목 | 호스트 전용 어댑터 (Host-Only) | NAT 네트워크 (NAT Network) |
| --- | --- | --- |
| **인터넷 접속** | **불가능 (X)** | **가능 (O)** |
| **호스트(내 PC)와 통신** | **가능 (O)** (직접 연결됨) | 원칙적으로 불가능 (포트포워딩 필요) |
| **VM 간 통신** | **가능 (O)** | **가능 (O)** |
| **외부 네트워크와 격리** | 완벽 격리 (폐쇄망) | 외부로 나갈 수 있으나, 외부는 내부를 못 봄 |
| **IP 할당 주체** | VirtualBox 자체 DHCP 서버 (Host-Only 인터페이스) | VirtualBox 내장 가상 라우터 (NAT 서비스) |
| **주요 용도** | **SSH 접속용 (Putty/Terminal)**, 보안 격리 테스트 | **패키지 설치(yum/apt)**, 외부 통신, 내부 LAN 구성 |

---

### 2. 호스트 전용 어댑터 (Host-Only Adapter)

이 모드는 가상 머신과 호스트(내 PC) 사이에 **'가상의 랜선'**을 하나 연결한 것과 같습니다. 인터넷과는 완전히 단절된 **폐쇄적인 내부 네트워크**를 형성합니다.

* **동작 원리:**
* 호스트 PC에 `VirtualBox Host-Only Ethernet Adapter`라는 가상의 네트워크 카드가 생성됩니다.
* VM들은 이 가상의 카드를 게이트웨이처럼 사용하여 호스트 및 동일한 어댑터에 연결된 다른 VM들과 통신합니다.


* **특징:**
* 외부 인터넷망(공유기 밖)에서는 이 VM에 절대 접근할 수 없어 보안상 안전합니다.
* 호스트 PC(내 컴퓨터)에서는 VM의 IP로 바로 `ping`을 날리거나 SSH 접속이 가능합니다.


* **활용:**
* 모의 해킹 실습 시 악성코드가 외부로 나가는 것을 방지할 때.
* DB 서버 등 외부 인터넷이 필요 없는 내부 백엔드 서버 구축 시.
* **가장 중요한 용도:** PuTTY, Xshell, VS Code Remote 등을 이용해 호스트에서 VM으로 편하게 접속하기 위함.



---

### 3. NAT 네트워크 (NAT Network)

기본 'NAT' 모드와 달리, 여러 대의 VM이 하나의 네트워크 그룹으로 묶여 **서로 통신**이 가능하면서 **인터넷도 사용**할 수 있는 모드입니다. 마치 집에 있는 **'유무선 공유기(Router)'** 아래에 여러 대의 PC를 물려놓은 것과 동일한 구조입니다.

* **동작 원리:**
* VirtualBox가 가상의 공유기(NAT Router) 역할을 수행합니다.
* VM들은 사설 IP(예: `10.0.2.x`)를 할당받고, 외부 인터넷으로 나갈 때는 호스트 PC의 IP를 빌려서(Masquerading) 나갑니다.


* **특징:**
* **VM끼리 통신 가능:** 기본 `NAT` 모드는 VM끼리 서로를 볼 수 없지만, `NAT 네트워크`는 같은 대역 안에 있어 서로 통신이 가능합니다.
* **외부 접속 차단:** 외부(인터넷)에서 VM으로 직접 들어오는 것은 차단됩니다(포트포워딩으로 해결 가능).


* **활용:**
* `yum`, `apt`, `wget` 등을 이용한 소프트웨어 설치 및 업데이트.
* 웹 서버 구축 후 외부 인터넷과의 통신 테스트.
* 여러 대의 서버(WEB-WAS-DB)를 하나의 사설 네트워크로 묶는 3-Tier 아키텍처 실습.



---

### 4. 실무/학습을 위한 권장 구성 (Best Practice)

대부분의 엔지니어링 실습(서버 구축, 쿠버네티스, 해킹 랩)에서는 **두 개의 랜카드(NIC)**를 장착하여 장점만을 취하는 방식을 사용합니다.

| 어댑터 순서 | 설정 모드 | 역할 |
| --- | --- | --- |
| **어댑터 1 (enp0s3)** | **NAT** (또는 NAT 네트워크) | **외부 통신용.** 인터넷 연결, 패키지 다운로드, 업데이트 담당. |
| **어댑터 2 (enp0s8)** | **호스트 전용 어댑터** | **관리 접속용.** 고정 IP를 부여하여 Putty/Terminal로 안정적인 SSH 접속 담당. |

> **구성 이유:** NAT만 쓰면 호스트에서 접속이 불편(포트포워딩 필요)하고, 호스트 전용만 쓰면 인터넷이 안 되어 패키지 설치를 못 하기 때문입니다. 이 **이중 구성(Dual Homed)**이 표준적인 랩 구성 방식입니다.

Next Step: 호스트 전용 어댑터를 활용한 고정 IP(Static IP) 설정 방법 (SSH 접속용)
