죄송합니다. 지적해주신 내용을 바탕으로 이전 대화의 맥락이나 수정을 언급하지 않고, 교재의 한 챕터처럼 완결된 형태로 작성했습니다.

---

# VirtualBox 네트워크 모드 심층 분석 및 구성 가이드

가상 머신(VM) 구축 시 가장 중요한 요소인 네트워크 구성 방식을 다룬다. VirtualBox는 다양한 네트워크 모드를 제공하며, 각 모드는 인터넷 연결 유무, 호스트(내 PC)와의 통신 여부, 외부 네트워크 노출 범위에 따라 명확한 차이를 가진다. 실습 환경의 목적에 맞는 올바른 네트워크 모드 선택이 필수적이다.

### 1. 4대 핵심 네트워크 모드 비교표

가상 머신을 외부 네트워크와 어떻게 연결할 것인지에 따라 아래 4가지 모드로 구분된다. 가장 큰 차이점은 **'격리 수준'**과 **'IP 할당 주체'**이다.

| 비교 항목 | NAT (기본값) | NAT 네트워크 | 호스트 전용 (Host-Only) | 브리지 어댑터 (Bridged) |
| --- | --- | --- | --- | --- |
| **인터넷 연결** | **가능 (O)** | **가능 (O)** | 불가능 (X) | **가능 (O)** |
| **VM ↔ VM 통신** | 불가능 (X) | **가능 (O)** | **가능 (O)** | **가능 (O)** |
| **Host PC → VM 접속** | 불가능 (포트포워딩 필요) | 불가능 (포트포워딩 필요) | **가능 (O)** (직접 접속) | **가능 (O)** (직접 접속) |
| **외부 PC → VM 접속** | 불가능 (X) | 불가능 (X) | 불가능 (X) | **가능 (O)** (완전 노출) |
| **IP 할당 주체** | VirtualBox 엔진 | VirtualBox 엔진 | VirtualBox 엔진 | **물리 공유기** (실제 라우터) |
| **IP 대역 예시** | `10.0.2.15` (고정) | `10.0.2.x` | `192.168.56.x` | `192.168.0.x` (실제 대역) |
| **보안 격리 수준** | **상 (개별 격리)** | **중 (그룹 격리)** | **최상 (폐쇄망)** | **하 (네트워크 노출)** |

---

### 2. 모드별 동작 원리 및 아키텍처

#### A. NAT (Network Address Translation)

VirtualBox 설치 시 기본적으로 설정되는 모드이다.

* **구조:** 가상 머신이 호스트 PC의 IP 주소를 빌려(Translation) 외부 인터넷과 통신한다. 각 VM은 자신만의 독립된 가상 라우터를 할당받는다.
* **특징:** 모든 VM이 `10.0.2.15`라는 동일한 내부 IP를 할당받기 때문에, 여러 대의 VM을 띄워도 서로를 식별할 수 없다. 외부에서는 VM의 존재를 알 수 없어 보안성이 높다.
* **용도:** 단순 OS 설치, 웹 서핑, 패키지 다운로드 등 단일 VM 운용 시 사용한다.

#### B. NAT 네트워크 (NAT Network)

기본 NAT 모드의 한계인 'VM 간 통신 불가' 문제를 해결하기 위해 고안된 모드이다.

* **구조:** 가상 공간 내에 하나의 **'논리적 공유기'**를 배치하고, 여러 VM을 그 하단에 연결하는 방식이다.
* **특징:** VM들은 `10.0.2.x` 대역의 사설 IP를 나눠 가지며 서로 통신할 수 있다. 외부로 나갈 때는 NAT 방식을 사용하므로 인터넷 연결도 가능하다.
* **용도:** Web-WAS-DB 3계층 아키텍처 실습, 쿠버네티스(Kubernetes) 클러스터 구축 등 다수 서버 간 통신이 필요한 환경에 적합하다.

#### C. 호스트 전용 어댑터 (Host-Only Adapter)

외부 네트워크(인터넷)와의 연결을 물리적으로 차단하고, 오직 **호스트 PC와 VM 간의 전용 통신 채널**만 생성하는 모드이다.

* **구조:** 호스트 OS에 `VirtualBox Host-Only Ethernet Adapter`라는 가상 랜카드가 생성되어 게이트웨이 역할을 한다. 랜선이 컴퓨터 내부에서만 연결된 폐쇄망(Closed Loop)과 같다.
* **특징:** 인터넷이 되지 않아 외부 해킹으로부터 가장 안전하다. 호스트 PC에서 VM으로 SSH 접속이나 핑(Ping) 테스트가 자유롭다.
* **용도:** 보안이 중요한 악성코드 분석 랩, 인터넷이 필요 없는 내부 DB 서버, 또는 관리용 터미널 접속 경로로 활용한다.

#### D. 브리지 어댑터 (Bridged Adapter)

가상 머신의 랜카드를 호스트 PC의 물리적 랜카드(이더넷 또는 Wi-Fi)에 직접 **'다리(Bridge)'**처럼 연결하는 방식이다.

* **구조:** 물리적 공유기(Router) 입장에서 볼 때, VM은 호스트 PC와 동등한 **'또 하나의 물리적 장치'**로 인식된다. 따라서 공유기로부터 실제 IP(Real IP)를 직접 할당받는다.
* **특징:** 네트워크 상의 다른 PC나 스마트폰에서도 이 VM에 접근할 수 있을 정도로 개방적이다.
* **용도:** 동료와 함께 서버 접속 테스트를 하거나, 실제 네트워크 환경과 동일한 조건에서 서비스를 배포해 볼 때 사용한다.

---

### 3. 실습 환경 구축 시 권장 구성

엔지니어링 실습에서는 단일 어댑터 대신 **두 개의 어댑터(Dual Adapter)**를 혼합하여 장점만을 취하는 구성을 표준으로 권장한다.

* **어댑터 1 (NAT):** 인터넷 연결을 담당하여 패키지 설치 및 업데이트를 수행한다.
* **어댑터 2 (호스트 전용):** 고정 IP를 할당하여 안정적인 SSH 원격 접속 및 내부 통신을 담당한다.

Next Step: 호스트 전용 어댑터를 활용한 고정 IP(Static IP) 설정 방법 (SSH 접속용)
