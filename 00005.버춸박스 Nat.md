# VirtualBox 핵심 네트워크 모드 3종 비교 분석

VirtualBox 네트워크 설정에서 가장 혼동하기 쉬운 **NAT**, **NAT 네트워크**, **호스트 전용 어댑터**의 차이점을 명확히 구분하여 정리했습니다. 각 모드는 **'인터넷 연결성'**과 **'통신 방향(누가 누구에게 접속 가능한가)'**에 따라 용도가 결정됩니다.

### 1. 3대 네트워크 모드 기능 비교표

| 비교 항목 | NAT (기본값) | NAT 네트워크 (NAT Network) | 호스트 전용 (Host-Only) |
| --- | --- | --- | --- |
| **인터넷 사용** | **가능 (O)** | **가능 (O)** | **불가능 (X)** |
| **VM ↔ VM 통신** | **불가능 (X)** (완전 격리) | **가능 (O)** (동일 서브넷) | **가능 (O)** |
| **Host PC → VM 접속** | 불가능 (포트포워딩 필요) | 불가능 (포트포워딩 필요) | **가능 (O)** (IP로 직접 접속) |
| **VM → Host PC 접속** | 가능 (O) | 가능 (O) | 가능 (O) |
| **IP 할당 방식** | 개별 사설 IP (모두 10.0.2.15) | 통합 DHCP (10.0.2.x 대역 공유) | VirtualBox 자체 DHCP |
| **주요 용도** | 단순 OS 설치, 웹 서핑 | 클러스터 구성, 서버 간 통신 실습 | SSH 터미널 접속, 폐쇄망 구축 |

---

### 2. 모드별 상세 동작 원리 및 토폴로지

#### A. NAT (Network Address Translation) - "독방 격리"

가장 기본이 되는 모드입니다. 가상 머신이 호스트 PC의 IP를 빌려 외부와 통신합니다.

* **구조:** 각 VM이 독립적인 `10.0.2.15`라는 동일한 IP를 부여받고, 자신만의 가상 라우터를 가집니다.
* **특징:**
* 옆에 있는 다른 VM도 똑같은 IP(`10.0.2.15`)를 가지므로, **VM끼리 서로를 식별하거나 통신할 수 없습니다.**
* 설정이 가장 간편하여 단순히 인터넷만 필요한 경우 사용합니다.


* **한계:** 서버 A가 서버 B로 핑(Ping)을 보내거나 데이터를 전송하는 실습이 불가능합니다.

#### B. NAT 네트워크 (NAT Network) - "가상 공유기"

하나의 가상 공유기(Router) 아래에 여러 VM을 연결한 구조입니다.

* **구조:** 모든 VM이 `10.0.2.0/24`와 같은 하나의 대역폭을 공유하며, 서로 다른 IP(예: `10.0.2.4`, `10.0.2.5`)를 할당받습니다.
* **특징:**
* **VM 간 통신이 자유롭습니다.** (WEB-WAS-DB 3계층 구조 실습 가능)
* 외부 인터넷도 호스트 PC를 통해 나갈 수 있습니다.
* 단, 호스트 PC(내 컴퓨터)에서 VM으로 들어갈 때는 여전히 포트포워딩 없이는 불가능합니다.



#### C. 호스트 전용 어댑터 (Host-Only Adapter) - "전용 직통 케이블"

인터넷 선을 뽑고, 호스트 PC와 VM 사이에만 LAN 케이블을 직접 연결한 것과 같습니다.

* **구조:** 호스트 PC에 가상 랜카드가 생성되어 VM들의 게이트웨이 역할을 합니다.
* **특징:**
* **외부와 완벽히 차단**되어 있어 보안상 안전합니다 (악성코드 분석 등에 적합).
* 호스트 PC에서 별도의 설정 없이 VM의 IP로 바로 **SSH 접속(Putty 등)**이 가능합니다.


* **한계:** 인터넷이 되지 않아 `yum`, `apt` 등의 패키지 설치 명령어가 작동하지 않습니다.

---

### 3. 실무 및 학습을 위한 최적의 구성 전략

엔지니어들이 랩(Lab) 환경을 구축할 때 가장 많이 사용하는 방식은 단일 모드가 아닌 **'혼합 구성(Dual Adapter)'**입니다.

#### [권장] 어댑터 2개 장착 (NAT + Host-Only)

이 방식을 사용하면 인터넷 연결과 편리한 원격 접속의 장점을 모두 취할 수 있습니다.

1. **어댑터 1 (NAT):**
* **역할:** 외부 인터넷 연결 담당.
* **용도:** `apt update`, `git clone`, `docker pull` 등 외부 리소스 다운로드.


2. **어댑터 2 (Host-Only):**
* **역할:** 내부 관리망 담당.
* **용도:** 고정 IP(Static IP)를 할당하여 Putty나 Xshell로 안정적인 터미널 접속 유지.



Next Step: 호스트 전용 어댑터를 활용한 고정 IP(Static IP) 설정 방법 (SSH 접속용)
