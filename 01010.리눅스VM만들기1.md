# VM 만들기

**VM OS : Linux**  

**브리지 어댑터 사용** : pc와 같은 네트워크 사용, 서로 통신 가능, 인터넷 가능 모드


**버춸박스 실행**

<img width="500" height="225" alt="image" src="https://github.com/user-attachments/assets/8faebba6-c137-4c3b-9452-0b20238c9b57" />

<img width="500" height="479" alt="image" src="https://github.com/user-attachments/assets/4808b1a6-e8c8-4ca1-a28f-59bbbe6b2aa3" />

<img width="500" height="530" alt="image" src="https://github.com/user-attachments/assets/3e82207f-1280-4860-b44b-78562af52089" />


<img width="500" height="499" alt="image" src="https://github.com/user-attachments/assets/a6a8c37e-7b83-4705-ba81-2a3f9048f6c0" />

<img width="500" height="692" alt="image" src="https://github.com/user-attachments/assets/5f00af7b-c32c-45ef-96fa-5d98e6ac246a" />

<img width="500" height="1045" alt="image" src="https://github.com/user-attachments/assets/4275e41b-8225-4f4e-8e8d-a22fe475ae18" />

<img width="500" height="467" alt="image" src="https://github.com/user-attachments/assets/4926965e-7799-429f-b9b2-787afbf7a1c8" />

<img width="500" height="548" alt="image" src="https://github.com/user-attachments/assets/d54c2739-e693-4a08-9b2f-133fdeb347c2" />

<img width="500" height="671" alt="image" src="https://github.com/user-attachments/assets/b059c1cd-0dbc-4209-b55f-db66a990bf3d" />

<img width="500" height="793" alt="image" src="https://github.com/user-attachments/assets/1d35b963-e6bc-432b-8ed8-d233fab1ea93" />

<img width="500" height="717" alt="image" src="https://github.com/user-attachments/assets/b80d46f8-20c2-4eb0-8c36-c750de8abbc0" />

<img width="500" height="469" alt="image" src="https://github.com/user-attachments/assets/27109f48-a78c-49fd-8113-05147dcd4ff1" />


**리눅스 설치 화면**

1. 언어선택 : 영어 or 한글(나중에 시스템 언어 바꾸는게 더 쉬움)

2. 이름, 호스트이름, 아이디,비번 다시 물음(자동설치가 완벽히 작동하지 않는듯)

3. 나머지는 모두 next 선택

4. 설치완료후 Reboot 화면

5. 로그인 화면
<img width="500" height="907" alt="image" src="https://github.com/user-attachments/assets/7d750081-9142-4683-b647-e7ac75759bca" />

   
7. 마우스가 올라가지 않을 땐 마우스 통합
<img width="400" height="276" alt="image" src="https://github.com/user-attachments/assets/33813549-7f75-46c3-83c1-8480055a4640" />

8. 시작안내 모두 skip, next

# ssh 서버 설치 및 가동하기

1. 터미널 열기 (서버에서는 그냥 작업)
2. ```
   $ sudo apt update
   $ sudo apt install openssh-server
   $ sudo systemctl status ssh
   $ sudo systemctl start ssh
   $ sudo systemctl enale ssh    ## 부팅시 자동 실행
   $ sudo ufw status
   if 활성화 되어 있다면,
   $ sudo ufw allow ssh 또는 sudo ufw allow 22/tcp
   $ sudo ufw reload    # 방화벽 재 적용 및 상태 확인
   $ sudo ufw status

   $ ip addr show  # 접속 IP 확인 : enp0s3

   #### 서비스 제어 명령
   $ sudo systemctl start ssh     # 중지된 서비스 시작
   $ sudo systemctl stop ssh      #실행 중인 서비스 종료
   $ sudo systemctl restart ssh   #설정 변경 후 적용 시 사용
   $ sudo systemctl status ssh    # 현재 작동 여부 확인
   ```
3. 네트워크 명령어 패키지 설치
   ```
   $ sudo apt update
   $ sudo apt install net-tools iputils-ping dnsutils
   $ which iproute2   # 설치여부 확인
   $ sudo apt install iproute2
   $ sudo apt install traceroute curl

4. 편리한 편집기 설치 및 설정
```
   $ vim --version
   $ sudo apt update
   $ sudo apt install vim
   $ vi .vimrc
     set number              " 줄 번호 표시 (필수)
     set cursorcolumn        " 현재 커서 위치에 세로줄 강조 (cuc)
     set cursorline          " 현재 커서 위치에 가로줄 강조 (십자선 효과)
     set showmatch           " 괄호 짝 찾기 강조 ({})
     set ruler               " 우측 하단에 현재 커서 좌표 표시
     :wq!
```

6. 커파일 위한 필수 패키지
   $ sudo apt update
   $ sudo apt install build-essential dkms linux-headers-$(uname -r) -y
   
