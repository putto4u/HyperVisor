# VirtualBox 성능 저하 원인 분석 및 완전 삭제(Clean Up) 가이드

VirtualBox에서 VM을 반복적으로 생성하고 삭제한 뒤 시스템이 느려지는 현상은 단순히 "기분 탓"이 아닙니다. 주된 원인은 **1) 삭제되지 않고 남은 대용량 디스크 파일(Zombie Disks)**과 **2) 디스크 단편화(Fragmentation)** 때문입니다.

VM 삭제 시 '지우기(Delete)'가 아닌 '제거(Remove)'를 선택했다면, 수십 GB에 달하는 가상 하드디스크 파일(.vdi)이 그대로 남아 호스트 PC의 저장 공간을 점유하고 I/O 성능을 떨어뜨립니다.

---

## 1. 가상 미디어 관리자를 통한 '고아(Orphaned) 디스크' 정리

VM 목록에서 우클릭으로 삭제했더라도, 실제 연결된 하드디스크 이미지는 보존되어 있을 수 있습니다. 이를 찾아 완전히 삭제해야 합니다.

1. **가상 미디어 관리자 실행:**
* VirtualBox 메인 화면 상단 메뉴 **[파일(File)]** > **[도구(Tools)]** > **[가상 미디어 관리자(Virtual Media Manager)]**를 클릭합니다. (단축키: `Ctrl + D`)


2. **연결되지 않은(Not Attached) 파일 식별:**
* 목록을 보면 각 디스크 파일 이름 앞에 아이콘이 있습니다.
* **[정보]** 탭이나 목록 상태창에 **'연결되지 않음(Not Attached)'**이라고 표시된 파일이 있는지 확인합니다. 이것들이 삭제되지 않고 용량만 차지하는 '고아 파일'입니다.


3. **완전 삭제 수행:**
* 해당 파일들을 선택하고 상단의 **[제거(Remove)]** 버튼을 누릅니다.
* 팝업창에서 반드시 **[삭제(Delete)]** (파일을 영구적으로 삭제)를 선택해야 합니다. ([보관(Keep)]을 누르면 목록에서만 사라지고 용량은 그대로 차지합니다.)



---

## 2. 호스트 PC의 디스크 최적화 (가장 중요한 단계)

VM은 하나의 파일이 수 GB에서 수십 GB에 달합니다. 이를 반복적으로 쓰고 지우면 호스트의 물리적 디스크에 빈 공간이 불규칙하게 생기는 **단편화**가 매우 심각하게 발생합니다. 이는 VM 생성/구동 속도를 급격히 떨어뜨립니다.

1. **Windows 드라이브 최적화 및 조각 모음:**
* Windows 시작 메뉴 검색창에 **'드라이브 조각 모음 및 최적화'**를 입력하고 실행합니다.
* VM이 저장되는 드라이브(보통 C: 또는 D:)를 선택하고 **[최적화(Optimize)]**를 클릭합니다.
* **SSD의 경우:** TRIM 명령을 수행하여 삭제된 공간을 정리해 속도를 회복시킵니다.
* **HDD의 경우:** 흩어진 데이터를 모아 물리적 헤드 이동을 줄여줍니다. (VM 성능에 큰 영향을 줍니다.)



---

## 3. 잔여 폴더 수동 삭제

VirtualBox는 VM 삭제 시 일부 로그 파일이나 스냅샷 폴더를 남겨두는 경우가 있습니다. 탐색기에서 직접 확인하여 지워야 합니다.

1. **기본 경로 확인:**
* 보통 `C:\Users\[사용자명]\VirtualBox VMs\` 경로에 VM 폴더들이 저장됩니다.


2. **잔여 폴더 삭제:**
* 이미 삭제했다고 생각한 VM의 이름으로 된 폴더가 남아 있다면, 해당 폴더를 통째로 **Shift + Delete** (완전 삭제) 합니다.



---

## 4. 전역 설정 파일 초기화 (선택 사항)

만약 위 방법으로도 해결되지 않고 VirtualBox 자체가 버벅거린다면, 설정 파일(`VirtualBox.xml`)이 꼬였을 수 있습니다.

1. **설정 파일 위치:** `C:\Users\[사용자명]\.VirtualBox\` 폴더로 이동합니다.
2. **파일 백업 및 삭제:**
* `VirtualBox.xml` 파일의 이름을 `VirtualBox.xml.bak`으로 변경합니다.
* VirtualBox를 다시 실행하면 초기화된 깨끗한 설정 파일이 새로 생성됩니다. (주의: 기존에 등록된 VM 목록이 초기화되므로, 필요한 VM은 '추가' 메뉴로 다시 불러와야 합니다.)



---

Next Step: 매번 OS를 설치할 필요 없이, 코드로 VM을 생성/삭제하고 깨끗하게 관리하는 'Vagrant' 도구 사용법
